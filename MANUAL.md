# One-Image Multi-Shot Director 사용자 매뉴얼

## 1. 이 스킬은 무엇인가

`one-image-multishot-director`는 Seedance 2.5용 독립 영상 연출 스킬입니다. 콘셉트에 따라 무드필름(`MOOD`)과 퍼포먼스 중심 영상(`PERFORMANCE`), 또는 두 방식을 섞은 `HYBRID` 모드로 연출합니다.

한 장의 기준 이미지(`@image1`)와 콘셉트, 무드, 원하는 영상 길이를 입력하면 다음 결과를 만듭니다. 아이돌·콘서트·댄스·액션처럼 움직임이 목적이면 `PERFORMANCE` 모드가 자동으로 선택됩니다.

1. 이미지에서 추출한 비주얼 앵커
2. 선택한 길이에 맞춘 4단 내부 타임라인
3. Seedance 2.5에 한 번에 붙여 넣는 영문 통합 프롬프트 1개

중요한 점은 **4단 구조와 4개 프롬프트는 다르다**는 것입니다.

- 4단 구조: 하나의 15~30초 영상 안에 들어가는 네 개의 연출 구간
- 통합 프롬프트: 네 구간을 한 번에 지시하는 하나의 프롬프트

이 스킬은 STORM, Notion, 특정 캐릭터 프로젝트에 의존하지 않습니다.

## 2. 기본 사용 흐름

### 입력 예시

이미지와 함께 다음처럼 요청합니다.

> 이 인물이 달빛 아래 수호자로 각성하는 영상으로 만들어줘. 30초.

공연·댄스·뮤직비디오처럼 움직임이 중요한 경우에는 다음처럼 요청합니다.

> 이 아이돌이 콘서트 무대에서 마이크를 휘두르며 조명 속 피날레 포즈를 만드는 영상으로 만들어줘. 15초.

또는 무드를 먼저 선택한 뒤 길이를 함께 적습니다.

> 3번 무드로 가고 20초로 만들어줘.

길이를 나중에 선택할 수도 있습니다.

> 3번 무드로 가고, 길이는 선택할게.

이 경우 스킬은 다음을 제시합니다.

1. 15초
2. 20초
3. 30초
4. 직접 입력

길이가 정해지기 전에는 최종 프롬프트를 만들지 않습니다.

### 이미지가 충분하지만 콘셉트가 모호한 경우

먼저 다음을 출력합니다.

- 이미지의 인물·의상·배경·조명 요약
- 성공 확률이 높은 3가지 장르·무드 제안
- 선택 번호와 추가 환경효과 입력 안내

이미지에 마이크, 악기, 춤 동작, 무대 조명처럼 퍼포먼스 단서가 보이면 세 방향 중 하나 이상을 `PERFORMANCE`로 제안하고, 각 방향의 동작 강도와 카메라 에너지를 구체적으로 구분합니다.

예시:

1. 달빛 암살자의 착지
2. 달빛 검무
3. 달의 수호자 각성

무드를 선택한 뒤 길이를 선택하면, 최종적으로 통합 프롬프트 하나를 받습니다.

## 3. 영상 길이 선택

### 모드 선택

- **MOOD:** 분위기, 관찰, 일상, 로맨스, 미스터리처럼 절제된 움직임이 어울리는 콘셉트
- **PERFORMANCE:** 노래, 춤, 콘서트, 액션, 변신, 축제처럼 화면의 운동 에너지가 중요한 콘셉트
- **HYBRID:** 조용한 시작 뒤에 각성·공개·추격처럼 분명한 전환과 행동이 이어지는 콘셉트

아이돌·콘서트·댄스·뮤직비디오를 선택하면 `PERFORMANCE` 모드를 사용합니다. 정체성 앵커는 유지하되, 포즈·팔 동작·머리카락·리본·소품·조명은 행동에 맞춰 변할 수 있습니다.

### 기본 선택지

- **15초:** 짧고 강한 티저·릴스 인트로
- **20초:** 무드와 행동을 균형 있게 보여주는 숏폼
- **30초:** 무드필름의 분위기 또는 퍼포먼스 영상의 상승·인서트·피날레를 충분히 담는 길이
- **직접 입력:** 10초, 12초, 25초 등 원하는 길이

### `MOOD` 기본 타이밍

| 전체 길이 | 구간 1 | 구간 2 | 구간 3 인서트 | 구간 4 |
|---:|---:|---:|---:|---:|
| 15초 | 0~2.5초 | 2.5~5.5초 | 5.5~10.5초 | 10.5~15초 |
| 20초 | 0~3초 | 3~7초 | 7~14초 | 14~20초 |
| 30초 | 0~5초 | 5~12초 | 12~22초 | 22~30초 |

이 표는 영상을 네 개로 나누어 따로 생성하라는 뜻이 아닙니다. **하나의 Seedance 생성 안에서 시간대별로 일어나는 장면 변화**입니다.

### `PERFORMANCE` 기본 타이밍

| 전체 길이 | 구간 1 훅 | 구간 2 상승 | 구간 3 짧은 인서트 | 구간 4 피날레 |
|---:|---:|---:|---:|---:|
| 15초 | 0~2초 | 2~5.5초 | 5.5~7.5초 | 7.5~15초 |
| 20초 | 0~3초 | 3~8초 | 8~11초 | 11~20초 |
| 30초 | 0~4초 | 4~13초 | 13~17초 | 17~30초 |

퍼포먼스 모드의 인서트는 전체 길이의 약 10~20%만 사용합니다. 남은 시간은 동작의 상승과 피날레에 배분합니다.

직접 입력한 길이는 같은 흐름을 유지하면서 타임코드를 다시 계산합니다.

## 4. 기준 이미지 준비법

Seedance가 기준 이미지를 안정적으로 확장하려면 다음 조건이 좋습니다.

- 가슴부터 머리까지 보이는 바스트 샷 또는 허벅지까지 보이는 미디엄 샷
- 인물 주변에 벽·거리·조명 같은 배경 여백이 30% 이상 있는 이미지
- 한쪽에서 들어오는 측면광 또는 외곽선을 만드는 역광
- 얼굴·헤어·의상이 선명한 이미지
- 손가락이 복잡하게 꼬이지 않은 자세
- 배경 잡동사니가 적은 이미지

퍼포먼스 모드라면 여기에 다음 조건이 특히 중요합니다.

- 팔·상체·다리 중 움직임을 설계할 수 있는 여유
- 마이크·검·가방처럼 동작에 사용할 수 있는 명확한 소품
- 머리카락·리본·천·조명처럼 움직임을 시각화할 요소
- 시작 포즈와 도착 포즈가 구분되는 이미지

다음 이미지는 불안정할 수 있습니다.

- 얼굴만 가득한 극단적인 셀카 클로즈업
- 손가락이 복잡하게 겹친 포즈
- 정면에서 균일하게 비추는 형광등
- 얼굴과 손을 가리는 소품
- 인물 주변 정보가 거의 없는 이미지

## 5. 비주얼 앵커란

비주얼 앵커는 통합 프롬프트 전체에서 유지해야 하는 기준값입니다.

앵커는 인물의 정체성·디자인·공간 연속성을 고정하는 장치입니다. 인물의 포즈나 모든 소품 위치를 정지시키는 명령이 아니며, `PERFORMANCE`에서는 행동에 따른 자세·소품·조명 변화를 허용합니다.

### Subject Anchor

- 얼굴 특징과 눈매
- 헤어스타일
- 기본 의상 실루엣
- 주요 색상
- 눈에 잘 띄는 액세서리
- 행동에 필요한 소품

### Environment Anchor

- 장소 구조
- foreground / midground / background
- 건물·창문·간판·난간 같은 고정 요소
- 인물이 움직일 수 있는 경로

### Lighting Anchor

- 광원의 방향
- 색온도
- 밝기와 그림자 방향
- 시간대

### Applied Safe Override

원본 인물 자체를 바꾸는 대신 환경과 조명만 조정합니다.

가능한 예:

- 비·눈·안개·연기·먼지
- 네온·골든아워·림라이트·달빛
- 배경 색온도와 노출 분위기

얼굴, 손가락, 세부 액세서리, 의상 구조를 구간마다 바꾸는 방식은 사용하지 않습니다.

## 6. 한 번에 만드는 4단 내부 구조

### `MOOD` 모드

1. **분위기와 공간 소개:** 인물이 어디에 있는지, 광원과 공간 구조가 무엇인지 보여줍니다.
2. **메인 감정:** 시선·표정·호흡·짧은 동작으로 감정을 확장합니다.
3. **매크로 인서트:** 손·소품·옷감·표면·빛 반사처럼 얼굴을 제외한 디테일을 보여줍니다.
4. **조용한 클라이맥스:** 마지막 포즈와 공간의 관계를 보여주며 마무리합니다.

### `PERFORMANCE` 모드

1. **즉시 훅:** 첫 0.3~0.5초 안에 마이크 들기, 고개 전환, 팔 동작, 첫 스텝, 조명 히트처럼 눈에 보이는 움직임을 시작합니다.
2. **동작 상승:** 자세·방향·위치·카메라 거리 중 하나를 분명하게 바꾸고, 머리카락·리본·의상·무대 조명이 동작에 반응하게 합니다.
3. **짧은 임팩트 인서트:** 손·마이크·발·소품·빛 반사·마스코트 등 행동과 연결된 디테일을 10~20%의 시간으로 보여줍니다. 단순한 정지 화면이 되지 않게 앞뒤 행동의 원인과 결과를 연결합니다.
4. **피날레 페이오프:** 마이크를 뻗기, 방향 전환, 큰 제스처, 조명 폭발, 위치 변화, 소품과의 상호작용처럼 시작과 다른 상태를 만든 뒤 히어로 구도로 마무리합니다. 단순히 포즈를 유지하는 것만으로 클라이맥스를 만들지 않습니다.

### `HYBRID` 모드

초반은 `MOOD`처럼 절제하되, 전환점 이후에는 `PERFORMANCE`의 동작 상승과 페이오프 규칙을 적용합니다.

### 인서트 사용 원칙

인서트는 얼굴 붕괴를 줄이기 위한 휴지기가 아니라 리듬과 원인·결과를 보여주는 장치입니다. 퍼포먼스 영상에서는 길게 늘이지 않고 앞뒤 행동과 연결합니다.

## 7. 최종 출력 형식

콘셉트와 길이가 확정되면 응답은 세 단계로 나옵니다.

### Step 1: Visual Anchor & Safe Override

한국어로 다음을 정리합니다.

- Subject Anchor
- Environment Anchor
- Lighting Anchor
- Applied Safe Override

### Step 2: Duration-specific Timeline

선택한 전체 길이에 맞춰 다음 내용을 표로 설명합니다.

- 네 개 내부 구간의 시간
- 구간 유형
- 카메라 움직임
- 화면에서 일어나는 행동
- 연출 의도

### Step 3: Single Master Prompt

영문 프롬프트는 **하나의 코드블록으로 하나만** 출력합니다.

프롬프트에는 다음 요소가 들어갑니다.

- 전체 영상 길이
- `referenced from @image1`
- 첫 프레임 구성
- 네 개 내부 구간의 타임코드
- 각 구간의 관찰 가능한 행동
- 구간별 카메라 무빙
- 고정된 비주얼 앵커
- 조명과 색보정
- 렌즈 느낌·필름 그레인·심도
- identity와 공간을 고정하는 연속성 문장
- 선택한 연출 모드와 에너지 곡선
- 퍼포먼스 모드라면 동작의 방향·크기·종료 상태와 무대의 반응

기본적으로 4개의 프롬프트를 만들지 않습니다. 별도 클립, 재생성, 편집용 소스가 필요하다고 사용자가 요청한 경우에만 분할 프롬프트를 제공합니다.

## 8. Seedance에서 렌더링하기

1. 원본 이미지를 `@image1`로 업로드합니다.
2. 생성된 통합 영문 프롬프트 하나를 붙여 넣습니다.
3. 원하는 전체 길이를 Seedance의 생성 길이와 일치시킵니다.
4. 한 번의 생성으로 전체 영상을 만듭니다.

이전 생성 영상의 마지막 프레임을 다음 영상의 기준 이미지로 사용할 필요가 없습니다. 통합 생성에서는 모든 구간이 같은 원본 이미지 앵커를 공유합니다.

이전 생성 프레임을 반복 사용하면 압축 노이즈와 identity drift가 누적될 수 있습니다.

## 9. 카메라 연출 원칙

각 내부 구간에는 하나의 주된 카메라 움직임만 사용합니다. `PERFORMANCE`에서는 움직임의 속도와 방향을 분명히 하되, 한 구간에 여러 축의 움직임을 겹치지 않습니다.

가능한 예:

- locked-off static camera
- slow push-in
- slow pull-back
- tracking shot
- slow pan
- subtle handheld follow

퍼포먼스 모드에서는 다음처럼 더 적극적인 움직임도 사용할 수 있습니다.

- decisive push-in
- lateral tracking
- rising crane
- short whip-pan into a new framing
- fast pull-back after a pose or prop hit
- controlled orbit around the performer

다음처럼 여러 움직임을 한 구간에 겹치지 않습니다.

> 줌하면서 회전하고, 동시에 빠르게 좌우로 팬한다.

카메라가 불안정해질 수 있으므로 하나의 주된 움직임과 명확한 속도만 지정합니다. 퍼포먼스 모드의 컷 뒤에는 새로운 화면 크기·각도·행동 상태·무대 반응 중 하나가 보여야 합니다. 단순한 컷 반복은 에너지를 만들지 않습니다.

## 10. CapCut으로 마무리하기

통합 생성이 끝난 뒤 CapCut은 선택적으로 사용합니다.

- BGM 추가
- 색보정과 필름 그레인 통일
- 시작·끝 부분 트리밍
- 자막이나 로고 추가
- 음악의 킥·스네어에 맞춘 내부 컷 조정

이미 통합 프롬프트 안에서 내부 컷이 구현되었다면, 영상을 네 개의 클립으로 다시 나눌 필요가 없습니다.

음악 파일이 제공되지 않은 경우에는 음악의 박자를 분석했다고 가정하지 않습니다. 이때는 시각적 리듬만 설계하고, 실제 음악을 선택한 뒤 편집에서 맞춥니다.

## 11. 최종 검수 체크리스트

- 요청한 전체 길이가 프롬프트에 정확히 들어갔는가
- 통합 프롬프트가 하나만 출력되었는가
- 기준 이미지와 인물 얼굴이 유지되는가
- 헤어스타일과 기본 의상이 유지되는가
- 배경의 핵심 구조가 유지되는가
- 조명 방향과 색온도가 갑자기 바뀌지 않는가
- 손·손가락·소품이 과도하게 복잡하지 않은가
- 세 번째 내부 구간이 얼굴 중심이 아닌 인서트로 구성되었는가
- 각 내부 구간에 카메라 움직임이 하나인가
- 네 구간의 화면 방향과 시선이 연결되는가
- 선택한 길이와 화면 비율이 맞는가
- 실제로 제공되지 않은 오디오·비디오를 분석했다고 말하지 않았는가
- `PERFORMANCE`라면 첫 0.3~0.5초 안에 움직임이 시작되는가
- `PERFORMANCE`라면 각 구간에 행동 방향·크기·종료 상태가 있는가
- `PERFORMANCE`라면 인서트가 전체의 10~20% 안팎이며 앞뒤 행동과 연결되는가
- `PERFORMANCE`라면 마지막에 시작과 다른 명확한 페이오프가 있는가
- 요청한 에너지와 반대로 `slow`, `gentle`, `subtle`, `hold` 같은 표현이 과도하게 반복되지 않는가

## 12. 이 스킬의 범위

이 스킬은 다음을 담당합니다.

- 이미지 기반 영상 연출
- 무드필름·퍼포먼스·하이브리드 모드별 영상 연출
- 원하는 길이에 맞춘 4단 내부 타임라인
- Seedance 2.5 통합 프롬프트 작성
- 초보자용 한국어 설명
- 영어 복사-붙여넣기 프롬프트 1개

이 스킬은 다음을 자동으로 하지 않습니다.

- Seedance에 직접 로그인하거나 렌더링
- 사용자가 제공하지 않은 영상·음악 분석
- 최종 영상 편집과 업로드
- STORM·Notion 프로젝트 구조 적용
